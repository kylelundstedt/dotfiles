# rclone exclude patterns for the home/ backup (relative to ~/)
# Caches / logs / regenerable state
Library/Caches/**
Library/Logs/**
Library/Saved Application State/**
Library/Application Support/CrashReporter/**
.Trash/**
.cache/**
.Trash/
# macOS-protected / app-internal state — needs Full Disk Access (denied otherwise),
# and the valuable data is redundant elsewhere, so excluded rather than granting FDA:
#   Messages -> archived by msgvault (~/archives/email); Mail -> IMAP server cache;
#   Contacts/Calendars/Notes/Safari -> iCloud; the com.apple.* containers are app state.
Library/Messages/**
Library/Mail/**
Library/Containers/**
Library/Group Containers/**
Library/Application Support/com.apple.*/**
# Dev regenerables
**/node_modules/**
**/.venv/**
**/venv/**
**/__pycache__/**
**/.mypy_cache/**
**/.pytest_cache/**
**/.next/**
**/dist/**
**/build/**
**/target/**
# Re-downloadable model weights (symlink target on external; excluded for safety)
.lmstudio/models/**
# Derived DuckDB rebuilt nightly from the source DBs (regenerable; ~127 MB)
/archives/hub/hub.duckdb
/archives/hub/hub.duckdb.tmp
# OS noise
**/.DS_Store
**/*.sock
# (rclone skips symlinks by default, so external-relocated dirs aren't followed)
