# AgentsView fleet-coverage exclusions — tailnet hosts that are online and Linux
# but legitimately do NOT run an AgentsView source. agentsview-coverage fails
# closed: any online Linux tailnet node that is neither a configured source nor
# listed here trips the check, so a new agent-capable VM cannot be silently
# uncollected. Add a host here ONLY if it genuinely has no agent history to
# collect (a service appliance), never to silence a host you simply forgot to
# enable.
#
# One hostname per line (the tailnet HostName).
#
# The last decommissioned appliance was llm-gateway, 2026-07-25 (fcacb8a). Add a
# host here only if it genuinely runs no agent harness.

# Rebuilt 2026-07-28 on the exeslim deployment lane: a Go binary plus two
# systemd units, no agent harness and no tailscale, so it collects nothing and
# is not a tailnet peer. Listed pre-emptively so that joining it to the tailnet
# later cannot trip the coverage check. Its 10 collected sessions stay in the
# archive. See agent_docs/vm-disk-weight.md.
rss-feed
