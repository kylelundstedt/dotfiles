# AgentsView fleet-coverage exclusions — tailnet hosts that are online and Linux
# but legitimately do NOT run an AgentsView source. agentsview-coverage fails
# closed: any online Linux tailnet node that is neither a configured source nor
# listed here trips the check, so a new agent-capable VM cannot be silently
# uncollected. Add a host here ONLY if it genuinely has no agent history to
# collect (a service appliance), never to silence a host you simply forgot to
# enable.
#
# One name per line. Matched against BOTH the tailnet HostName and the exe.dev
# VM name, since coverage now enumerates the exe.dev inventory as well as
# online tailnet peers — a VM that never joined the tailnet is not a peer and
# was previously invisible to this check entirely.
#
# The last decommissioned appliance was llm-gateway, 2026-07-25 (fcacb8a). Add a
# host here only if it genuinely runs no agent harness.

# Rebuilt 2026-07-28 on the exeslim deployment lane: a Go binary plus two
# systemd units, no agent harness and no tailscale, so it collects nothing and
# is not a tailnet peer. Listed pre-emptively so that joining it to the tailnet
# later cannot trip the coverage check. Its 10 collected sessions stay in the
# archive. See agent_docs/vm-disk-weight.md.
rss-feed

# quack substrate experiment, created 2026-08-21 on stock boldsoftware/exeuntu.
# Never joined the tailnet, so coverage sees them only via the exe.dev inventory
# pass. Zero Claude and zero Codex sessions as of 2026-08-26; quack-client holds
# ~/quack-experiment, quack-server an empty home.
#
# NOTE this is a weaker excuse than rss-feed's: the stock image ships .claude and
# .codex, so unlike a service appliance these CAN accumulate agent history, and
# it would go uncollected. Excused as short-lived experiment boxes on the
# understanding that they are deleted when the experiment ends — if either starts
# doing real agent work, enroll it instead of leaving this entry in place.
quack-client
quack-server
