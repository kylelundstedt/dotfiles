# TODO

Open work only, grouped by when it can happen. Completed work →
[CHANGELOG.md](CHANGELOG.md). Runbooks, status tables, decisions, and plans →
[agent_docs/](agent_docs/README.md).

## Actionable now

- [ ] `ssh exe.dev ls --json` is unreliable enough to starve the checks that depend on it — `agentsview-coverage` failed on `exe.dev inventory unavailable after 2 attempts` **578 times** between 2026-07-28 (when the inventory pass landed) and 2026-08-26, i.e. essentially every hourly run for a month, with only two successes; `entire-push-check` failed the same way daily. Both recovered on their own 2026-08-26 13:39. Worth checking whether the hourly cadence is self-inflicting the exe.dev SYN-drop lockout documented in `AGENTS.md` — if so, back the inventory call off to daily, or cache it. Note what this cost: a month of alerts on the probe hid the real coverage gap (four uncovered hosts) that the check exists to find.
- [ ] `check-monitoring.sh` asserts a check's schedule, grace and channel but **not that it has ever been pinged** — `agentsview-retention` sat at `n_pings=0` from 2026-07-28 to 2026-08-26 while reporting no drift (the ping call itself was malformed; fixed in this batch). Same family as the "wired to no channel" lesson in [agent_docs/monitoring.md](agent_docs/monitoring.md): configuration was right, arrival was never checked. Consider failing the drift check on a manifest check whose `n_pings` is 0 or whose `last_ping` is older than its period + grace.
- [ ] Delete `quack-client` / `quack-server` when the quack substrate experiment ends, and drop them from `provisioning/agentsview-coverage-exclude.txt` — they are excused as experiment boxes, but the stock image ships `.claude`/`.codex`, so agent history there would go uncollected while the excuse stands.
- [ ] Update the `apple-containers` skill to the machine-mode approach — it predates this work and still uses `container run` + `ubuntu`; align it with [agent_docs/apple-container-vms.md](agent_docs/apple-container-vms.md)
- [ ] `exe-dev` **skill's** "Setting Up a Dev VM" still documents a registered `new.setup-script` default, but `defaults read dev.exe new.setup-script` returns `(not set)` — following it yields a VM waiting on a bootstrap that never runs. [agent_docs/exe-dev.md](agent_docs/exe-dev.md) already records the no-hook contract correctly; the skill is the stale copy, and it's the one agents read first. (The companion defect in `model-routing-economics.md` was corrected 2026-07-22.)
- [ ] AgentsView — **ADOPTED 2026-07-28**, pilot closed. Retention mechanism now exists (`maint/.local/bin/agentsview-retention`, weekly, 90-day window, exports to dated Parquet under `~/archives/agentsview/retired/` before pruning). Remaining: ~~(a) create the `agentsview-retention` healthchecks.io check~~ **done 2026-07-28** (daily check, Keychain URL stored, manifest row added, `check-monitoring: no drift`) — but it **never actually pinged** until 2026-08-26: both success paths passed `--data-raw` as job_hc's path argument, so every ping went to `hc-ping.com/<uuid>--data-raw` and 404'd silently. Fixed 2026-08-26; the check reads `up` for the first time. Config was right the whole month, arrival was never asserted; (b) **token fan-out** is still hand-placed `source.env` per host (12 mirrored in 1P `Employee` → "AgentsView fleet tokens"; `iv-cli` enrolled 2026-08-26 from the token already on the VM and is **not yet mirrored** — 13th); (c) `agentsview prune` left 2 of 13 eligible sessions behind on the first run — the script now warns rather than hiding it, but the vendor's `--before` semantics are not understood. See [agent_docs/agentsview-pilot.md](agent_docs/agentsview-pilot.md)
  - **DEMOTED 2026-09-02:** the global collector moved to the `iv-agentsview` exe.dev VM (peer integration `agentsview`, bearer at the exe.dev edge); the mini is now a plain source daemon on `:8080`, pulled as the 13th `[[remote_hosts]]`. Follow-ups: (a) the mini's `sessions.db` still carries frozen collector-era mirrors of the fleet (last sync 2026-09-02 19:07Z) — prune or ignore; (b) ~~`agentsview-coverage` now self-skips on the mini and has no home on the collector yet~~ **moved to the collector 2026-09-06** (`provisioning/iv-agentsview/`, daily systemd timer, inventory via the `ls`-only `api-exe-ls` integration) — its first run found **three exeslim-dev VMs created 2026-08-29..09-03 and never enrolled: `fannie-sflpd-poc`, `iv-cmg`, `aom-build`**; the check fails daily until they get a source daemon + `[[remote_hosts]]` entry (or, better, the peer-integration data path below); (c) item (b) above (token fan-out) is now the collector VM's problem, not the mini's — and exe.dev **peer integrations** could retire it entirely: one `--peer` http-proxy per source VM attached to `iv-agentsview`, sources bound to loopback behind their own exe.dev auth proxy, caller identified by `X-Exedev-Source-Vm`, no token on any VM (the mini, not an exe.dev VM, keeps the tailnet path). Note the mini's launchd jobs are stow symlinks into `~/dotfiles`, so **the checked-out branch is live** — keep the mini on `master`.
- [ ] herdr + Moshi pilot — **phone path proven 2026-07-18** (Moshi iPhone → mini via `sshd:2222` → herdr; `herdr-layout` helper deployed fleet-wide). Remaining: detach/reattach + `--takeover` semantics check, one week of real use, then retire Shelley carve-outs. Plugin options surfaced (collie phone web-PWA, herdr-reviewr, moshi-hook notifications) — see [agent_docs/herdr-pilot.md](agent_docs/herdr-pilot.md)
- [ ] VM disk weight — **prune + deployment lane DONE 2026-07-28**; ~10.1G reclaimed fleet-wide (mini 3.10G, iv-docs/kgl-thoughts 1.64G each, rest smaller) and `rss-feed` rebuilt on `ghcr.io/kylelundstedt/exeslim:2026-07-28.1.1` at **268M vs 7.8G**, feed output byte-identical to baseline. Remaining: (a) decide the **recurring** prune mechanism — `maint/.local/bin/prune-disk` exists and is stowed fleet-wide but nothing schedules it, and the Claude versions dir regrows on every update; settle it with the AgentsView retention item above. (b) a **deploy profile** for install.sh before a second service joins the lane. (c) evaluate a **slim base for dev VMs** — measured 2-3G/box of exeuntu that nothing touches, incl. duplicate aws-cli (533M) and gh (44M); blocked on making provision-iv.sh self-bootstrap from a bare base. See [agent_docs/vm-disk-weight.md](agent_docs/vm-disk-weight.md)
- [ ] exe.dev remediation — **Track 0 (control plane) DONE 2026-07-28**: blanket `ForwardAgent` on `*.ts.net` removed (it forwarded the exe.dev control key into every VM; `ssh iv-docs 'ssh exe.dev whoami'` returned the account identity), VM-side `ControlPersist` for `exe.dev` removed (an authenticated socket outlived the session by 10 min and was reusable with no key), and `tailscale-api` detached from `auto:all` + `vm:telnyx-vm` (was HTTP 200 from public `rss-feed`, now 403). `join-tailnet` rewritten to attach-then-detach. Remaining: (a) **decide the `github-mcp-home`/`github-mcp-work` cohorts** — both live at `auto:all` though `secrets.md` documents narrower intent; they are live MCP servers on 4 VMs so a blind detach breaks agent tooling; (b) `motherduck-mcp` on `tag:iv`, `telnyx-test` stray on `iv-home`, dead `fannie-token` tag attach, `llm` double-attach; ~~(c) **Track 1**~~ **DONE 2026-07-28** — `prune-disk --system` added (`/tmp` by age, journal vacuum, apt cache) with a guard that protects `/tmp` entries holding repos with a real remote and unpushed commits; applied at 7d then 3d fleet-wide, plus `~/.cache/pip` (2.7G each on `iv-home` and `telnyx-vm`), for **~7.8 GB** total; fleet now 68.9G instantaneous. `/tmp` held no local-only work after all — the apparent hits were `--all` counting fetched tags plus stale remote-tracking refs, both over-reporting; guard 3 corrected to `rev-list --branches HEAD --not --remotes`. Remaining follow-up: **nothing schedules `prune-disk`**, so caches and the Claude versions dir regrow — settle alongside the AgentsView retention timer. Also: `maint` is stowed on only 6 of 9 VMs; `iv-entire-agent-shelley`, `iv-foundry-stage2` and `telnyx-vm` have no `~/dotfiles` clone at all; ~~(d) **Track 2**~~ split: **2a DONE 2026-07-28** — aws moved to on-demand `install-cloud-cli` (2.9G; no VM had aws credentials), shadowed duplicate binaries removed (2.0G; the stale copies were exeuntu's, not ours), `pi` dropped (1.09G). Fleet 68.9G → **63.1G**. **2b open** — exeslim-dev image (~250-300 MB vs exeuntu's ~4 GB, so ~3.7 GB/VM saved); the blocking premise was wrong and is corrected (iv-image a107dcc): `LABEL exe.dev/install-shelley=true` opts Shelley back in. **Untested** — needs one canary to confirm Shelley runs, `/headless-shell` arrives, and `git`/`jq`/`unzip` suffice for provision-iv.sh. `/usr/local/go` (269 MB/VM) is base-image, so it is a 2b item. Defer `telnyx-vm` until the number port completes. Note `/headless-shell` (2.25G fleet-wide) is **not** reclaimable — it is wired into `shelley.service`. See [agent_docs/exe-dev-remediation.md](agent_docs/exe-dev-remediation.md)
- [ ] **Purge the now-excluded agent-harness objects from `bkup:home`.** The filter change (2026-08-02) stops future syncing of `~/.claude`/`~/.codex` credentials, paste-cache, shell-snapshots, file-history and transcripts — but **rclone applies filters to both source and destination**, so ~6,400 already-uploaded objects are now ORPHANED in the bucket rather than deleted. They will sit there indefinitely unless removed explicitly. **Correction 2026-08-03:** this item previously claimed the orphans include the transcript carrying the SHARED TOTP seed. They do not — that transcript was never uploaded (the 08-02 nightly predated the leak and the 08-03 nightly ran with the new exclusion). This purge is therefore ordinary hygiene over stale OAuth tokens, `file-history`, `paste-cache` and `shell-snapshots` uploaded **before** 2026-08-02, and it does **not** remediate the seed — see the Snowflake SHARED MFA item. **Priority target added 2026-08-04:** the msgvault Gmail credentials — `archives/email/tokens/**` and `archives/email/msgvault-{home,work}-client-secret.json` — were excluded on 2026-08-03/04 but the copies uploaded **before** those rules are orphaned in the bucket. Those are live mailbox credentials and should be purged **first**, ahead of the stale OAuth caches. Enumerate with `rclone lsf bkup:home/archives/email/ -R`, `rclone lsf bkup:home/.claude/ -R` and `… bkup:home/.codex/ -R`, purge with `--dry-run` first; the bucket's 30-day soft-delete then applies to the removal. Until this runs, the filter change is prevention only, not cleanup
- [ ] **Decide whether `~/Downloads` belongs in the Tigris home backup.** `backup/tigris-backup-filter.txt` is a denylist and carries no `/Downloads/**` rule, so anything a browser writes there is backed up by default. Surfaced 2026-08-03: `config.meshcore.io`'s Export button writes the repeater's **private key in cleartext JSON** (confirmed in the tool's source, `src/gui.js:525-527` — it explicitly queries `prv.key` and adds it to the blob), and the file sat in `~/Downloads` as `config-repeater-*.json`. **Nothing was exposed** — it was moved to 1P and deleted the same evening, hours before the 04:30 nightly, so it never reached the bucket. But that was a manual catch, and it is the **third instance of this class in three days**: `~/.claude/file-history` (08-01), the TOTP seed via `~/archives/agentsview` (08-02), this (08-03). The common shape is a secret landing in an included directory by a route nobody modelled. Not an obvious fix — `~/Downloads` may hold real data worth backing up, so the options are wholesale exclusion, a narrower pattern (e.g. `config-*.json`, `*.p8`, `*.pem`), or a pre-sync scan that fails the run instead of uploading. Note the standing caveat at the top of the filter file: excluding a path later leaves already-uploaded objects **orphaned** in the bucket rather than deleting them, so decide before something lands rather than after
- [ ] **Decide whether `ALLOW_CLIENT_MFA_CACHING` should be `true` on the Snowflake SHARED account.** It is set at ACCOUNT level and is **non-default** (Snowflake's default is `false`), so MFA authentications are cached and re-enrolling MFA does not necessarily invalidate an already-cached session. Surfaced 2026-08-04 while re-enrolling after the TOTP-seed leak — it is the reason "the old seed is dead the instant we re-enroll" is slightly too strong. Caching the second factor on a break-glass ACCOUNTADMIN is a real trade (convenience for automated logins vs. a longer window where a compromised factor still works); it should be a deliberate choice rather than an inherited setting. Check whether MAIN / CMG_SDW / SHARETEST carry it too. See CHANGELOG 2026-08-04
- [x] **Recreate cycle for dev VMs — MIGRATION COMPLETE 2026-08-01.** All **7 of 7** dev VMs are on `exeslim-dev:2026-07-29.6.1`; fleet disk **65.8 → 32.7 GB**. Every retirement passed the full gate sequence (repos clean+pushed, no ungit'd trees, collector entry removed BEFORE deletion, canonical name restored, integration attachments moved) and `fetch` was verified afterwards rather than assumed. `kgl-thoughts` and `telnyx-vm` stay on exeuntu by decision; `rss-feed` was already on exeslim.
  - **Tooling RETIRED 2026-08-23** (`new-dev-vm`, `bump-vm-image`, `retire-vm`, `recreate-fleet`, `fleet-refresh`). Creation moved to the `create-vm` skill in `iv-provision`, which runs from any browser via a `--cmds=new,ls,whoami,"integrations list"`-scoped token — no laptop, no 1Password unlock, no hostname guard. Retirement is a runbook (`iv-provision/retiring.md`), deliberately manual: deletion is the one fleet operation where a mistake is unrecoverable. `recreate-fleet` was self-described transitional and its migration finished 7/7. `fleet-refresh` depended on the two deleted scripts and its weekly launchd job is unregistered.
  - **The image pin no longer needs bumping by hand, and image age stopped being the driver.** `iv-apt-upgrade.timer` patches every VM daily, so a VM on an older image is not a stale VM — measured 2026-08-23: `iv-gitlake` on a 2026-07-29 image was AHEAD of `iv-cli` on 2026-08-19 (curl `.12` vs `.11`, nginx `.17` vs `.16`, all 179 other packages identical). Recreates are now justified by image _content_ changes, not patch currency.
  - The hard-won findings below still hold and are why the runbook exists — read them before any recreate.
  - Original notes below.
- [ ] ~~**Recreate cycle for dev VMs**~~ — decided 2026-07-29. Nothing on these VMs patches itself (`apt-daily` masked by both exeuntu and exeslim; `iv-docs` measured at **77 pending security updates** on a 43-day-old VM), and exe.dev fixes the image at creation, so **recreate is the maintenance model, not an exception**. `maint/.local/bin/new-dev-vm` automates the expensive part — create → tailnet join → `provision-iv.sh` → dotfiles overlay → AgentsView token _and_ matching collector entry → work repos → verify (incl. the control-plane escalation probe with `ControlPath=none`). Deleting the old VM stays manual: `--recreate` prints a retirement checklist.
  - **FIRST REAL CUTOVER DONE 2026-07-29: `iv-ave-adapters-next`** (from `iv-ave-adapters`), exit 0. Image `exeslim-dev:2026-07-29.6.1`. Disk **2.7 GB** (2.2 base + Quarto) vs ~6.9 GB on the exeuntu original; 0 failed units; AgentsView enrolled; control plane correctly denied. Capability set narrowed from 3 integrations + 2 tags to **1 integration (`repo-ave-adapters-rw`) + 1 tag (`mcp-agent`)**.
    - **Render verified content-identical.** 27 files each; all 3 HTML pages differ by exactly 2 lines, every one the content-addressed `bootstrap-<hash>.min.css` filename — the replacement runs Quarto 1.9.38, the original an older build. Zero other differences, byte-identical sizes. Baseline was re-rendered fresh first (a stale one produced a bogus "35% regression" earlier in this work).
    - **Compare manifests with `LC_ALL=C`.** The two boxes sort differently, which made `README.html`/`TODO.html` look like file-set changes when the sets were identical.
    - **`iv-ave-adapters` is NOT deleted** — retirement is a separate decision; the script printed the checklist.
  - **DECIDED 2026-07-29 (resume here):** (a) **interleave retirement** — recreate one, verify its render, retire it, then move to the next, rather than batching retirement at the end. Reason: the pooled disk was at **74.3 GB of 100 GB** with only 3 duplicates standing, and exe.dev bills `fs_used_gb`, which runs far higher than `df` (e.g. `iv-gitlake-examples` bills 10.71 GB). Seven duplicates at once would crowd the pool. (b) **`kgl-thoughts` is out of this batch** — public site, decide separately.
  - **Done so far (3 of 7), all originals still running and untouched:** `iv-ave-adapters-next` (old billed 6.62 GB → new 0.64), `iv-gitlake-examples-next` (10.71 → 0.64), `iv-gitlake-next`. Each: 0 failed units, AgentsView enrolled, control plane denied, renderer present. Render diff verified for **ave-adapters only** — `gitlake-examples` and `gitlake` still need theirs before retirement.
  - Retire with `maint/.local/bin/retire-vm <old> --replaced-by <new>` (collector entry first, then `--delete` explicitly).
  - **PROGRESS 2026-07-29: 4 of 6 recreated AND retired** — `iv-ave-adapters`, `iv-gitlake-examples`, `iv-gitlake`, `iv-home`. Each on exeslim-dev at ~2.2–2.7 GB (originals were 3.5–9.4), canonical names restored, collector entries migrated, coverage green, fleet back to 10 VMs. Renders verified content-identical on the three doc sites (only the Quarto-version `bootstrap-<hash>.min.css` filename differs).
  - **BLOCKED: 1Password on the mini is locked** — `new-dev-vm` preflight refuses because the agent lists the exe.dev key but cannot sign with it. Unlock on `klundstedt-mini` and re-run. Nothing was half-created; the refusal happens before any control-plane call. **This will recur on any unattended cycle** — decide whether the cron run should skip when the agent can't sign, or notify; today it does neither.
  - **Three blind spots found while retiring, all now guarded** — each was a check that was correct about what it measured and silent about what it didn't:
    1. **Gitignored bulk**: `iv-gitlake-examples` had 1.0 GB of `tpch-mortgage/out*` (ignored) plus 1.5 GB in `/tmp`, invisible to `git status` and to a 1.9 MB `.git`. Regenerable by design, so it retired normally — see [agent_docs/vm-disposability.md](agent_docs/vm-disposability.md).
    2. **Git worktrees**: `iv-home` had `~/iv-home-dataset-mcp`, a worktree on branch `dataset-mcp`. A worktree's `.git` is a **file**, so `new-dev-vm`'s `[ -d .git ]` inference never sees one and the replacement doesn't get it. `retire-vm` now reports worktrees and refuses if any has uncommitted/unpushed work.
    3. **Tailnet name collision**: deleting a VM does not free its tailnet node (ephemeral reaping lags ~30 min), so the renamed replacement registered as `iv-ave-adapters-1` while the collector pointed at the dead node — and **coverage still read OK**, because it validates configuration, not scrapes. `join-tailnet` now clears a stale claimant first; `retire-vm` re-registers via logout + join (note `tailscale set --hostname` does NOT rename a registered node).
  - **`disk_used_gb` from exe.dev is unreliable** — it claimed 14.29 GB for `iv-home` where `df` said 6.3 GB, and `fs_used_gb` froze at 0.638 for every new VM. Trust per-VM `df`; treat pool totals as rough.
  - **`iv-foundry-stage2` — replacement BUILT; the retirement hold is RESOLVED 2026-07-29 and the VM is retirable.** `iv-foundry-stage2-next` is healthy (2.2 GB, 0 failed units, enrolled). The earlier claim — "~31 MB that exists nowhere else" — was wrong in both directions, and the whole 31 MB is either gitignored output or byte-identical to commits already on the remote:
    1. `~/worktrees/iv-foundry-stage2` was measured at 29 MB, but **24 MB of that is gitignored Quarto output** (`_site` 11 MB, `_site-internal` 13 MB) plus a 3.7 MB `.quarto` cache — all three named in that tree's own `.gitignore`, so outside the invariant (see "Gitignored generated output" in [agent_docs/vm-disposability.md](agent_docs/vm-disposability.md)). Source is **2.3 MB / 132 files**.
    2. Those 132 source files are **byte-identical to `origin/iv-foundry-stage2`** (`d301642`, _"docs: reconcile repaired P1-S1 runtime origin"_, 2026-07-26) **on the `kylelundstedt/iv-docs` repo** — 0 absent, 0 differing, compared by `git hash-object`. The tree is a stale copy of `iv-docs`, not a separate project; a sibling branch `origin/iv-foundry-stage2-reconciled` also exists.
    3. `ave-adapters-p1-s1-m6` (1.6 MB) is byte-identical to the git-backed `ave-adapters-m6-native` worktree on the same VM at `b5e7c4c` — clean, 0 local-only commits, contained in `origin/main` **and** `origin/p1-s1-m6-foundry-reference-acceptance`. The only difference is a `.pytest_cache` the git-backed copy has and this one lacks.
    4. `entire-agent-shelley-v0.1.0` (168 KB, of which 68 KB is `__pycache__`) hash-matches **every file** of the pushed `v0.1.0` tag on `iv-entire-agent-shelley` (`~/entire-agent-shelley`, clean, 0 local-only); the tag additionally carries a `.github/workflows/test.yml` this copy lacks. `sha256sum -c SHA256SUMS` passes.
    - **No content decision is needed** — retire it whenever the agent can sign. Its 7 git-backed clones were already clean and pushed; exact commits (incl. two detached-HEAD pins at `b5e7c4c`) in [agent_docs/foundry-stage2-worktrees.txt](agent_docs/foundry-stage2-worktrees.txt). Method in [agent_docs/vm-disposability.md](agent_docs/vm-disposability.md) → "Ungit'd is not the same as unpushed".
  - **VALIDATED 2026-07-30: first replacement that came up WITH THE WORK.** Re-ran `iv-foundry-stage2` on the rewritten tooling; exit 0, 2.7 GB, 0 failed units, control plane denied, and all **7 checkouts reproduced exactly** — right paths, right commits, right branches. Both pinned entries stayed detached at `b5e7c4c`; the two branches that no longer exist upstream (`p1-s2-m2-package-bootstrap`, `p1-s2-m2-completion-m3-start`) were recreated locally at their recorded SHAs by `checkout -B`. The same VM's first migration produced only `dotfiles` and `iv-image` and was retired that way.
  - **Parallel recreates: multiplexing is NOT the blocker.** `ControlMaster` is already on for `exe.dev`/`*.exe.xyz` on the Mac (that is why install.sh keeps it there and strips it from VMs), so lobby traffic already shares one TCP connection. The two real gates are (a) `new-dev-vm` phase 5's unlocked read-modify-write of the collector TOML — needs `flock`; (b) the GitHub API's 60/hr-per-egress-IP limit against ~10 `install_github_binary` calls per install. The clean fix for (b) keeps the token OFF the VM behind an exe.dev API proxy, like `tailscale-api` — setting `GITHUB_TOKEN` on a disposable box reintroduces exactly what track 0 removed. Cap concurrency at 2–3 regardless; the lockout came from rapid repeated attempts and the threshold is not worth probing on a live fleet.
  - **STATUS 2026-07-30 — `iv-docs` replacement BUILT, retirement REFUSED: the original has unpushed work.** `iv-docs-next` is healthy (2.7 GB, 0 failed units, enrolled, control plane denied, renderer correctly names `iv-docs`). But the original carries **3 unpushed commits and 1 untracked file**: `main` +1 (`5b3f5eb` _"docs: compare Hotdata with IV Platform"_), `entire/checkpoints/v1` +2, and untracked `index-gfm.md` (not gitignored). **Push or discard before retiring.** Found only because the two VMs rendered differently — the original is at `5b3f5eb` while the replacement cloned `origin/main` @ `23ff52b`; the original is _ahead_, not behind.
    - **This was the biggest hole in `retire-vm`, and the most basic one.** Its check tested `.git` as a _file_ — worktrees only — so an ordinary clone with unpushed commits was never examined. Elaborate checks existed for gitignored bulk, worktrees and ungit'd trees while the obvious case went unchecked. Fixed in `1e83ac4`: scans depth 3 (`~/repo`, `~/worktrees/<name>`, `~/github/<owner>/<repo>`), matches `.git` as file or directory, counts every local branch, and refuses outright. **The four VMs already retired were checked under the weaker rule** — they were clean, but by luck rather than coverage.
  - **`iv-foundry-stage2` RETIRED 2026-07-30**, replacement renamed to the canonical name — **but it is OFF THE TAILNET**. 1Password locked mid-operation, so the post-rename `hostnamectl` + `tailscale logout` + re-join never ran. The VM is running and healthy; it needs one `join-tailnet iv-foundry-stage2` once the agent can sign. Coverage stays red until then. Also note the edge SSH needed `ssh-keygen -R iv-foundry-stage2.exe.xyz` first — the deleted VM's host key was still cached and `accept-new` does not override a _changed_ key.
  - **Transient integration errors are the norm, not the exception.** exe.dev's integration plumbing is eventually consistent on first use: `tailscale-api` answers 403 right after an attach (seen 3×, retry fixed it), and the github proxy answered "repository not found" for a repo whose integration was attached at creation, then served it fine minutes later. Both now retry. With no `--resume-from`, a single transient error wastes a fully provisioned VM.
  - **`iv-docs` — replacement PARTIAL, retry queued.** The run died cloning `entire-agent-shelley` (403): `iv-docs` still holds `~/entire-agent-migration`, a checkout of a repo whose integration was deliberately detached from `iv-docs` earlier the same day. Fixed in `5f59d00` — inference now skips repos nothing grants on the replacement (direct, tag, `auto:all`, or public). **Verified 2026-07-29 that dropping the checkout loses nothing:** it is 828 KB, `main` @ `8c4e72b`, clean, 0 local-only commits, contained in `origin/main`, and the dedicated `iv-entire-agent-shelley` VM holds the repo properly (`~/entire-agent-shelley`, HEAD `11f72f1`, clean, 0 local-only, tag `v0.1.0`). `iv-docs` never needed that integration — it was capability accumulation, and the narrowing was right. The partial `iv-docs-next` must be deleted before retrying (no `--resume-from`; it never reached phase 5, so there is no collector entry to clean). All 10 of its checkouts are clean and pushed — the ~700 MB that looked like ungit'd bulk is `.venv`, regenerable from `uv.lock`; the repos sit one level deeper at `<name>/adapters/.git`. Manifest: [agent_docs/iv-docs-worktrees.txt](agent_docs/iv-docs-worktrees.txt).
  - **The recurring blind spot: nested layouts.** `new-dev-vm` infers work repos from `~/*/` only. `iv-foundry-stage2` keeps everything in `~/worktrees/`, `iv-docs` in `~/worktrees/` **and** `~/github/kylelundstedt/`, some nested a further level under `<name>/adapters/`. So the replacement can come up with **no work at all** and every existing check still passes. "The replacement is healthy" and "the replacement has the work" are different claims and only the first is verified.
  - **A fourth blind spot, NOT yet guarded: "ungit'd" was never tested against sibling repos.** The `iv-foundry-stage2` hold stood on three checks that were each correct and each insufficient — `[ -d .git ]` found no repo, `kylelundstedt/iv-foundry-stage2` does not exist on GitHub, and a depth-2 rescan found no nested repos. None asked whether the _content_ was already committed, and it was: on a branch of `iv-docs` named after the VM. **Proposed check for `retire-vm`:** before reporting a tree as ungit'd, `git hash-object` its non-ignored files and look for a matching remote branch — or a historical blob — in every repo cloned on the same VM. That settled this case completely, including the one file absent from `iv-docs`' tip (blob `ea34587c`, dropped by `9d13bbb "chore: graduate Shelley Entire plugin"`, still reachable from `origin/main` at `b65d85c`).
  - **Commit signing is NOT the blocker.** `git log --format=%G?` returns `N` on this repo and commits/pushes succeed with 1Password locked (HTTPS + `gh` credentials). What stops everything is the exe.dev **SSH auth** key in the same vault. Removing commit signing would not help; keeping 1Password unlocked, or relocating that key, would.
  - **Exposure math:** image staleness at birth + cycle interval. On our exeslim fork (rebuilt weekly) that is ~7 + X days; on exeuntu (boldsoftware's cadence, ~monthly, not ours) ~30 + X. Argues for adopting exeslim-dev _with_ the cycle.
  - **What the cycle does NOT patch:** the version-pinned tool layer in `provision-iv.sh` (duckdb, quarto, tigris, rclone, herdr, agentsview, apex) and `install-cloud-cli`'s aws/azure/gcloud — a recreate reinstalls identical pins. Those need deliberate bumps; consider Renovate on iv-image.
  - **`telnyx-vm` is excluded** — mid-port, irreversible carrier process, registered webhook endpoint. Needs real patching or an accepted-risk note.
  - Suggested X ≈ 30 days once the first run proves the script.
  - **STATUS 2026-07-29: `new-dev-vm` COMPLETED A FULL END-TO-END RUN** on `iv-canary-d` — all seven phases, including the four (join, AgentsView enrollment, work repos, verify) that had never executed. Verify reported: provision lock written, `agentsview-source` **active** and enrolled in the collector, **0 failed units**, **disk 2.2 GB**, and `control-plane reachable from VM: no (correct)` — the last one a _real_ assertion now that the image ships an ssh client.
    - It still exited 1, from the **last line only**: `agentsview-coverage --dry-run` correctly FAILed because the earlier throwaway canaries `iv-canary-b` and `iv-canary-c` are agent-capable tailnet hosts that are neither enrolled nor excused. `set -euo pipefail` propagates that. **Fail-closed monitoring doing its job, not a script defect** — it clears when those two are deleted or excused.
    - Fixes that made this run possible are below; each one my earlier diagnosis got wrong at least once.
    1. ~~**exeslim-dev ships no `openssh-client`**~~ — **RESOLVED 2026-07-29, and the diagnosis was wrong.** `install.sh` was not calling `ssh` at all. Two heredocs writing `~/.ssh/config` were unquoted (to expand `$LOCAL_BIN` in the `Match exec` line) and their prose comments contain backticks, so bash **command-substituted them**: `ssh exe.dev whoami` on the Linux block, `ssh iv-docs 'ssh exe.dev whoami'` on the macOS one — the exact control-plane call track 0 closed, re-run by every install on any box that has a client. Fixed in `bd8e8c3` (quote both delimiters, `printf` the one expanding line). `openssh-client` **was still added** to `Dockerfile.dev` (`7f6c26c`) for a different and real reason: `/exe.dev/bin` is inbound-only (`dtach exe-init sftp-server sh sshd sshd-session`, verified on iv-docs), so the VM-to-VM stanzas both provisioners write are dead config without a client. ~2 MB. **Still needs: rebuild + re-pin `IMAGE_DEFAULT` in `new-dev-vm`.**
    2. ~~**Tailscale auto-auth failed**~~ — **RESOLVED 2026-07-29; it was never a failure.** `tailscale-api` is deliberately not attached at VM creation (least authority — the control plane attaches it only for the join and detaches after), so the OAuth probe hits a non-JSON error page. The `2>/dev/null` in that pipeline bound to `curl`, not `jq`, so the parse error leaked and the fallback message misdescribed it. `new-dev-vm` phase 4 joins via `join-tailnet.sh`, which does the attach/join/detach correctly. Fixed in `bd8e8c3`: redirect `jq` too, and say what is actually happening.
    3. ~~**Provisioned-VM size is UNMEASURED**~~ — **MEASURED 2026-07-29 on `iv-canary-b`: `df -h /` reports 2.0 GB used**, fully provisioned (exeslim-dev base + `provision-iv.sh` + dotfiles `install.sh`, no work repos), 0 failed units, `shelley.socket` active. The earlier ~2.2 GB arithmetic was close. Live exeuntu dev VMs for comparison: `iv-docs` 10.0 GB, `iv-home` 7.2 GB, `iv-gitlake` 6.9 GB (exe.dev `fs_used_gb`). **≈5–8 GB saved per dev VM.**
       - Breakdown: `/home` 1.1 G — `.local/share/claude` 263 M, `.local/share/uv` 234 M, `.local/share/fnm` 212 M (node), then `.local/bin`: carapace 78 M, uv 53 M, `op` 40 M, yq 14 M. `/usr` 812 M, `/var` 76 M.
       - Next trims, if wanted: carapace at 78 MB is the single largest binary and only supplies shell completions; `op` (40 M) is only needed where 1Password CLI is actually used.
    4. ~~**`new-dev-vm` has not completed end-to-end**~~ — **DONE** on `iv-canary-d`, see status above. Note `disk: 2.2 GB` here vs 2.0 GB on `iv-canary-b`; the difference is python3's shims plus codex, both of which were missing from the earlier box.
    5. ~~**`join-tailnet` 403**~~ — **RESOLVED, and the real cause was neither the proxy nor a race.** `~/.agents/skills/join-tailnet/join-tailnet.sh` on the mini is a **regular file installed from GitHub by `npx skills add`**, not a stow symlink to the working tree — so the retry fix committed to dotfiles never reached the script `new-dev-vm` actually executes. The second run failed with the _old_ error text, which is what gave it away. Re-ran `npx -y skills add -g -y kylelundstedt/dotfiles -s sprites-dev join-tailnet upgrade-vm`, after which the join succeeded first try.
       - **Standing gotcha:** editing a skill in `dotfiles/agents/.agents/skills/` changes nothing on this Mac until it is committed, pushed, **and** re-installed via npx. Local working tree ≠ what runs.
       - `upgrade-vm` fails to install: `PromptScript does not support global skill installation`. Pre-existing, unrelated, still broken.
    6. **NEW — the two throwaway canaries `iv-canary-b` and `iv-canary-c` must be deleted or excused**, or `agentsview-coverage` keeps FAILing (and `new-dev-vm` keeps exiting 1). `iv-canary-b` was never enrolled; `iv-canary-c` was never enrolled. `iv-canary-d` IS enrolled, so deleting it later also means removing its `[[remote_hosts]]` block from `~/.agentsview/config.toml`.
  - Fixed during that run and already committed: `new-dev-vm` joined the tailnet _before_ running `install.sh` (which is what installs Tailscale — chicken-and-egg), and used the MagicDNS short name for phases that run before the VM is on the tailnet (must use `$NAME.exe.xyz`). Order is now create → provision-iv.sh → dotfiles → tailnet → AgentsView.
  - **SSH discipline bit us:** repeated `*.exe.xyz` attempts while debugging triggered exe.dev's per-source-IP SYN dropping and connections began hanging. One attempt at a time, 30–60 s apart; use `ssh exe.dev stat <vm>` (the lobby) to inspect a VM without touching the edge.
- [ ] **PARKED 2026-07-29 — Quarto → lowdown site migration.** `render-md-site` (iv-image `bin/`) renders a markdown repo to `_site` mirroring the repo tree; `render-site` dispatches to it unless a repo has a `.render-with-quarto` marker. **All five sites currently carry that marker, so nothing has changed** — Quarto still renders everything. Removing a marker is the deliberate switch. Motivation: Quarto is 423 MB and no site uses a computational feature; lowdown is 268 KB and apt-managed.
  - **Resume by** rendering to `_site_apex`, then comparing against a **FRESH** `quarto render` with `iv-image/tests/sitediff.py` (word-level + structural; filename parity is not sufficient — it passed while pages were silently losing content). A stale baseline produced a bogus "35% regression" once; always re-render Quarto first.
  - **State:** page sets match exactly on all four (gitlake 10, ave-adapters 3, gitlake-examples 8, iv-docs 77). 73 of 98 pages content-identical; ~13 differ only in link counts (Quarto's `#cbN-M` code-block line anchors, not content); **13 have real text differences left to characterise** — likely a few systematic causes.
  - **lundstedt.us (`thoughts`) stays on Quarto permanently** — public, and holds the fleet's only Pandoc fenced div (`::: {#home-recent}`).
  - **Do not go back to apex.** Three silent content-loss bugs on this corpus, all exit 0 with well-formed HTML: paragraphs >1024 chars truncated; a list + table + later `::` deletes the table; a heading ending in `[...]` loses that text or vanishes entirely. Reported upstream as [ApexMarkdown/apex#31](https://github.com/ApexMarkdown/apex/issues/31).
  - **Payoff when done:** Quarto drops off 4 VMs (~1.7 GB). See [agent_docs/exe-dev-remediation.md](agent_docs/exe-dev-remediation.md)
- [ ] Doc-site search — the one capability `render-md-site` (apex) does not replace from Quarto. `gitlake`'s `_quarto.yml` had `search: true`; the apex renderer gives sidebar + per-page TOC but no full-text index. **Before building JS search, decide whether an IV MCP server is the better answer**: these sites already co-emit `format: gfm` markdown twins and an `llms.txt` index specifically "so AI agents can consume the site as markdown rather than HTML" (iv-docs `_quarto.yml`), which means the primary consumer may be agents, not browsers. An MCP server over the markdown would serve that consumer directly and make in-page search a human-only nicety rather than a blocker. Options: extend `hub-mcp` (personal-mcp), or a per-repo docs MCP alongside `provision-docsite`. See [agent_docs/exe-dev-remediation.md](agent_docs/exe-dev-remediation.md) → Track 2a/quarto
- [ ] Split `setup_agents` and `setup_git` into focused functions; verify with `./test-install.sh overlay` + `exe`
- [ ] Tailscale admin console: tighten `tag:dev` SSH users (drop `root`/`autogroup:nonroot` → `exedev`); consider `*:22` instead of `"ip": ["*"]` in the network grant

## Waiting on a trigger

- [ ] Decide whether to retire `iv-home` — explicitly deferred 2026-07-19. It currently serves the closed-door corporate repo read-only on the tailnet only; do not change or delete it until this decision is revisited.

- [ ] Mobile access to hub-mcp: pointing a phone MCP client at the tailnet URL **cannot work** — claude.ai/mobile connectors originate from the vendor's cloud, not the device (see personal-mcp README "iPhone / iPad"). Real options: SSH-to-mini pattern (covered by the herdr pilot) now; OAuth on the server + Tailscale Funnel later (tracked in personal-mcp TODO.md). Reconnect the iPhone's Tailscale app regardless (offline since ~2025-11)
- [ ] Confirm `claude` is installed and individually logged in on all seven exe.dev VMs (verified on `iv-docs` 2026-07-22: v2.1.218, `~/.claude/.credentials.json` present). This is the **supported** way to get Claude Max onto the fleet — running Claude Code, the native client, per VM. `install.sh` distributes no Claude credentials, so each VM needs its own login. This is the supported replacement for the withdrawn Shelley dual-provider plan (see [agent_docs/shelley-dual-provider.md](agent_docs/shelley-dual-provider.md), closed 2026-07-22)
- [ ] Per-project app secrets on VMs: 1P service account + `op run --env-file` — **mechanism VERIFIED 2026-07-22** on a throwaway canary (evidence + gotchas in [agent_docs/shelley-dual-provider.md](agent_docs/shelley-dual-provider.md) "1Password service-account delivery"): SAs are available on the plan, `op run` resolves non-interactively with only `OP_SERVICE_ACCOUNT_TOKEN`, systemd `ExecStart=op run … -- <binary>` works, and it fails closed (missing token → unit never executes; bad reference → exit 1 with a clear journal line). Gotcha: SAs require an explicit `--vault` on `op item get`. `install.sh`'s Linux `op` install needs no change. Remaining: confirm rotation directly against a writable item (needs a writable test item — the spike SA was read-only), decide vault/SA-per-project granularity, add `OP_SERVICE_ACCOUNT_TOKEN` support to install.sh, then validate on a real project. **First real consumer is now AgentsView token fan-out** (line above); the dual-provider rollout that originally motivated it was withdrawn 2026-07-22
- [ ] provision-iv.sh (iv-image): merge `~/.claude/settings.json` instead of overwriting — only if the upgrade-vm skill's re-run-the-overlay step proves error-prone in practice
- [ ] exe.dev multi-tenant build-out — gated on first paying client; decisions + ordered checklist in [agent_docs/multi-tenant.md](agent_docs/multi-tenant.md)

## Evaluations (no deadline)

- [ ] Basic Memory: pick hosting model (local/Cloud/Teams), trial 1–2 weeks on one project, then adopt-or-drop with a policy note distinguishing it from Claude/Codex native memory

- [ ] **Revisit commit signing.** The 1Password agent dependency is a recurring
      operational headache — it locks, and every signing-dependent operation
      stops. Raised 2026-07-29 while the recreate cycle was blocked by a locked
      agent. Decide whether commit signing is worth keeping, and if so how it
      should behave unattended. (Note: the exe.dev control-plane key is SSH
      _auth_, a separate concern from git commit signing — but both ride the
      same 1Password agent, so both stop together.)
